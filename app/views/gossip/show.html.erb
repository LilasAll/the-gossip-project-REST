
<div class="card">
  <div class="card-header">
    <div class="font-weight-bold text-center"><h1>Gossip n°<%=params[:id]%></h1></div>
    <div class="text-muted text-center"><%Gossip.find(params[:id]).tags.each do |item| %> 
    	Tag: #<%=item.title%>
    	<%end%>
    	</div>
  </div>
  <div class="card-body">
    <h5><strong>Titre : </strong><%= Gossip.find(params[:id]).title%><br/>
    <strong>Contenu du Gossip : </strong><%= Gossip.find(params[:id]).content%></h5>
    <h5>Tu veux checker la page de l'auteur de ce potin ? 
    <a hclass="btn btn-info"<%= link_to "#{User.find(Gossip.find(params[:id]).user_id).first_name} #{User.find(Gossip.find(params[:id]).user_id).last_name}", author_path(Gossip.find(params[:id]).user_id.to_i)%></a></h5>
  </div>
</div>
